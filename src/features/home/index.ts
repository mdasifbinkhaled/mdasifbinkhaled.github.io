/**
 * Home feature — public API
 *
 * All homepage section components are exposed here so consumers
 * can import from `@/features/home` instead of deep paths.
 */
export {
  HeroSection,
  NewsSection,
  ResearchHighlights,
  PublicationsPreview,
  ExperiencePreview,
  FeaturedGrant,
  ConnectSection,
} from './components';
